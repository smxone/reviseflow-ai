# ReviseFlow

Never lose your learning flow.

Full specs live in `docs/` (kept local, not pushed to GitHub — see `.gitignore`). This README will be expanded into the full submission-style version later, per `CLAUDE.md`.
