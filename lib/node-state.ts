// Derives a roadmap node's 5-state status (Unstarted/Learning/Shaky/Mastered/Fading) from profile() + staleness signal.
// TODO: implement per IMPLEMENTATION_SPEC.md §8 — finalize predicates after Spike B (forgetting).
